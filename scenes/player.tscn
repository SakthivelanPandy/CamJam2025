[gd_scene load_steps=6 format=3 uid="uid://cekpla2ei26xj"]

[ext_resource type="Texture2D" uid="uid://g7dbrm1tfsan" path="res://assets/rectangles.png" id="1_6mru5"]

[sub_resource type="GDScript" id="GDScript_fb5g7"]
script/source = "extends CharacterBody2D


const SPEED = 300.0

func _ready() -> void:
	add_to_group(\"player\")
	print(get_tree().current_scene.name)

func _process(delta):
	var velocity = Vector2.ZERO # The player's movement vector.
	if Input.is_action_pressed(\"move_right\"):
		velocity.x += 1
	if Input.is_action_pressed(\"move_left\"):
		velocity.x -= 1
	if Input.is_action_pressed(\"move_down\"):
		velocity.y += 1
	if Input.is_action_pressed(\"move_up\"):
		velocity.y -= 1

	if velocity.length() > 0:
		velocity = velocity.normalized() * SPEED
		
	if get_tree().current_scene.name == \"PerfumeHouse\":
		if (position + velocity * delta).x > 450 or (position + velocity * delta).x < -450:
			velocity *= -0.01
		if (position + velocity * delta).y > 500 or (position + velocity * delta).y < -500:
			velocity *= -0.01
	
	position += velocity * delta
"

[sub_resource type="AtlasTexture" id="AtlasTexture_1tq0w"]
atlas = ExtResource("1_6mru5")
region = Rect2(200, 212, 200, 106)

[sub_resource type="SpriteFrames" id="SpriteFrames_igjpc"]
animations = [{
"frames": [{
"duration": 1.0,
"texture": SubResource("AtlasTexture_1tq0w")
}],
"loop": true,
"name": &"default",
"speed": 5.0
}]

[sub_resource type="CircleShape2D" id="CircleShape2D_llapq"]
radius = 122.723

[node name="Player" type="CharacterBody2D"]
script = SubResource("GDScript_fb5g7")

[node name="AnimatedSprite2D" type="AnimatedSprite2D" parent="."]
sprite_frames = SubResource("SpriteFrames_igjpc")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(-1, 1)
shape = SubResource("CircleShape2D_llapq")
